# Optional: used by scripts/capture-screenshots.js for automated user-guide screenshots.
# Copy to .env in this directory and set values. Do NOT commit .env or real passwords.
# Use test accounts only.

PLAYWRIGHT_BASE_URL=http://localhost:5173
PLAYWRIGHT_CLIENT_EMAIL=
PLAYWRIGHT_CLIENT_PASSWORD=
PLAYWRIGHT_STAFF_EMAIL=
PLAYWRIGHT_STAFF_PASSWORD=
